
<h3>
  bands
</h3>

<%= render 'shared/errors' %>
<a class="llinks" href="<%= new_band_url %>" >add band</a>

<div class="bands_list">
  <ul>
    <% @bands.each do |band| %>
    <li>
      <a href="<%= band_url(band) %>"><%= band.name %></a>
    </li>
    <% end %>
  </ul>
</div>